[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn command_center.cc_backend:app --bind 0.0.0.0:$PORT --workers 2"
healthcheckPath = "/cc/setup-check"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
